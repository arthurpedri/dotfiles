return {
  -- 'hrsh7th/nvim-cmp',
  -- config = function()
  --   local cmp = require 'cmp'
  --   cmp.setup {
  --     sources = cmp.config.sources {
  --       { name = 'codeium' },
  --       -- Keep the existing sources
  --     },
  --   }
  -- end,
}
